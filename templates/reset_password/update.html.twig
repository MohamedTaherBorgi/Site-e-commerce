{% extends 'security/register.html.twig' %}

{% block content %}
    {#    <form method="post"> #}
    {% for message in app.flashes('notice') %}
        <div class="alert alert-info">{{ message }}</div>
    {% endfor %}
    {{ form_start(form) }}

    {{ form_row(form.new_password) }}

    <center>
        {{ form_row(form.submit) }}
    </center>

    {{ form_end(form) }}
    {#        <h1 class="h3 mb-3 font-weight-normal">Réinitialiser mon mot de passe</h1> #}
    {#        <label for="inputEmail">Email</label> #}
    {#        <input type="email" name="email" id="inputEmail" class="form-control" #}
    {#               autocomplete="email" required autofocus> #}
    {#        <br> #}
    {#        <div style="display: flex; justify-content: space-between;"> #}
    {#            <button class="btn btn-lg btn-primary" type="submit">Réinitialiser</button> #}
    {#            <a href="{{ path("app_login") }}" style="font-weight: bold; font-size: 17px;">Connecter a votre compte</a> #}
    {#        </div> #}
{#    </form>#}
{% endblock %}
