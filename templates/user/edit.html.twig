{% extends 'user/index.html.twig' %}

{% block content %}
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                <h1>Edit User</h1>

                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_user_index') }}" class="btn btn-dark">back to list</a>
            </div>
        </div>
    </div>
{% endblock %}
